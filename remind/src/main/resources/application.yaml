spring:
  application:
    name: remind
  datasource:
    url: jdbc:postgresql://localhost:5433/art_db
    username: admin
    password: super_secret_password_123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true