spring:
  application:
    name: remind
  datasource:
    url: jdbc:postgresql://localhost:5432/art_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true